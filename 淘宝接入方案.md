# 淘宝/天猫客服消息接入方案（ai-cs-agent）

> 目标：让 ai-cs-agent 的 6 Agent 客服系统，接入淘宝/天猫店铺的客服消息渠道（千牛客服/店小蜜场景之外的标准化方案），自动回复买家咨询。
> 状态：方案稿，含【待确认】项，等你拍板后再落地代码。

---

## 一、先厘清：淘宝客服接入的「三条赛道」，别走错门

很多人一说"接入淘宝客服"就去找 `open.taobao.com` 的私信接口。这条路对电商客服**勉强能走但门槛最高**。先把可走的几条赛道分清楚：

| 赛道 | 网址 / 文档 | 面向谁 | 和客服消息的关系 |
|---|---|---|---|
| 淘宝开放平台 TOP API | `open.taobao.com` / `developer.alibaba.com` | ISV / 工具型 / 自用型应用开发者 | **客服消息主路径**：订阅 `taobao_openim_ChatMessage` 事件接收买家消息，调用 `taobao.openim.custmsg.push` 主动回复 |
| 千牛开放平台 | `qianniu.open.taobao.com`（PC 客户端 JSAPI） | 千牛工作台插件方 | 嵌入千牛工作台侧边栏，调用 JSAPI 获取当前接待用户；客服插件定制 2024 后已停用 |
| 阿里云 OpenAPI + 连接器 | `next.api.aliyun.com` / `developer.alibaba.com` 的连接器体系 | 云服务 / BC 消息 | 2024 后逐步统一的 API；客服消息卡片按"场景"申请权限（虚拟商品发货 / 改地址 / 延迟发货等），场景化开放 |

**结论**：你的系统是独立客服系统（不是千牛插件、也不是阿里云连接器走场景卡片），应该走 **TOP API 路径**，直接订阅买家消息事件 + 主动发客服消息。

---

## 二、三条可走的路，只有一条对你最合适

| 路径 | 做法 | 门槛 | 结论 |
|---|---|---|---|
| **A. TOP API 消息推送 + 接收（标准 HTTP webhook）** | 淘宝开放平台创建「自用型应用」→ 申请客服消息权限包 → 订阅 `taobao_openim_ChatMessage` 事件 → webhook 收到买家消息 → 调 `taobao.openim.custmsg.push` 回复 | 营业执照 + 淘宝/天猫店铺 + 自用型应用审核 + 客服消息权限包 + 公网 HTTPS 地址 | ✅ **推荐**（与现有架构最贴合，复用 `run_chat`） |
| B. 千牛插件 JSAPI | 写一个 JS 插件嵌入千牛工作台侧边栏，调用 `QN.application.invoke` 获取当前接待用户 → 调你后端 → 把回复写回千牛工作台（实际是给客服看，不是给买家） | 千牛开发者账号 + 应用审核（客服插件已停用，需走新机制） | ⚠️ 备选（适合做"坐席辅助"，不是"买家自动回复"） |
| C. 千牛后台 RPA | 浏览器脚本/插件模拟客服操作千牛工作台 | 无官方接口 | ❌ 不推荐（封号风险、灰色、非正规） |

**推荐路径 A 的理由：**
1. TOP API 是淘宝/天猫官方对 ISV 的标准开放路径，合规、可规模化。
2. ai-cs-agent 现有 6 Agent（订单/商品/售后/政策）职能与淘宝买家典型咨询场景天然匹配。
3. webhook 模型与抖店方案高度对称，复用 `run_chat(msg, session_id)` 只需新增一层适配器，**代码量最小**。
4. 已有 TOP API 完整支持订单/商品/物流查询（`taobao.trade.get` / `taobao.items.list.get` / `taobao.logistics.companies.get`），未来要扩展"主动调订单详情查 Triage 工具"也不用换协议。

---

## 三、接入架构（核心原则：不侵入现有 Agent 逻辑）

现有 `/api/chat` 已经封装了完整链路「消息 → Triage 分流 → 业务 Agent → 回复 + trace + 转人工」。淘宝接入**只加一层适配器**，把淘宝消息格式（加密 + 签名）翻译成内部格式，复用同一套 Agent 逻辑：

```
淘宝买家发消息（千牛客服 / 店铺客服）
        │
        ▼  淘宝「消息服务」webhook POST（需公网 HTTPS + 验签 + AES 解密）
┌──────────────────────────────────────────────────────┐
│ taobao_webhook.py   ← 新增，接收淘宝推送                  │
│   1. verify_signature()   验签（防止伪造推送）           │
│   2. aes_decrypt()        AES-256-CBC 解密消息体        │
│   3. parse_message()      解析买家消息（open_id + 文本）  │
│   4. 调 run_chat(msg, session_id) 复用现有逻辑          │
│   5. taobao_send_message() 调 TOP API 回发回复           │
└──────────────────────────────────────────────────────┘
        │                        ▲
        ▼                        │
   复用 main.py 的 Runner.run(triage_agent, ...)  → 返回 reply
```

**关键复用点**：把 `/api/chat` 里那段「`Runner.run(triage_agent, msg, context=ctx, max_turns=10)` + 异常处理 + trace 提取」抽成独立函数 `run_chat(msg, session_id)`，webhook 和 REST 接口共用，避免逻辑分叉。

**会话映射**：淘宝的「会话 ID / 买家 open_id / session_id」→ 内部 `session_id`。复用现有 `_sessions`（30 分钟过期）+ 转人工 `_manual_sessions` 机制，实现跨轮次上下文保留。

**多租户对齐**：淘宝的「店铺 ID（seller_nick）」天然对应现有 `tenant_id`，知识库按店铺隔离；OAuth 授权拿到的 `access_token` 按店铺缓存。

---

## 四、关键代码骨架（真实协议，落地时按官方文档补字段）

### 4.1 OAuth 2.0 授权 + access_token 管理

淘宝自用型应用走标准 OAuth 2.0：店铺主扫码授权 → 拿 `code` → 换 `access_token` + `refresh_token`。`access_token` 有效期通常 7~30 天，`refresh_token` 用于续期。

```python
import time, httpx

class TaobaoAuth:
    def __init__(self, app_key: str, app_secret: str):
        self.app_key = app_key
        self.app_secret = app_secret
        self._tokens: dict[str, dict] = {}  # shop_id -> {access_token, refresh_token, expire}

    async def exchange_code(self, code: str, shop_id: str) -> dict:
        """授权码换 token（首次接入时调用一次）"""
        resp = await httpx.AsyncClient().post(
            "https://oauth.taobao.com/token",
            params={
                "grant_type": "authorization_code",
                "code": code,
                "client_id": self.app_key,
                "client_secret": self.app_secret,
                "redirect_uri": "https://your-domain.com/taobao/oauth/callback",
            },
        )
        data = resp.json()
        self._tokens[shop_id] = {
            "access_token": data["access_token"],
            "refresh_token": data["refresh_token"],
            "expire": time.time() + data.get("expires_in", 86400 * 7) - 300,
        }
        return data

    async def get_access_token(self, shop_id: str) -> str:
        """懒获取 + 自动 refresh"""
        tok = self._tokens.get(shop_id)
        if not tok or time.time() >= tok["expire"]:
            await self._refresh(shop_id)
            tok = self._tokens[shop_id]
        return tok["access_token"]

    async def _refresh(self, shop_id: str):
        tok = self._tokens[shop_id]
        resp = await httpx.AsyncClient().post(
            "https://oauth.taobao.com/token",
            params={
                "grant_type": "refresh_token",
                "refresh_token": tok["refresh_token"],
                "client_id": self.app_key,
                "client_secret": self.app_secret,
            },
        )
        data = resp.json()
        self._tokens[shop_id].update({
            "access_token": data["access_token"],
            "refresh_token": data.get("refresh_token", tok["refresh_token"]),
            "expire": time.time() + data.get("expires_in", 86400 * 7) - 300,
        })
```

### 4.2 webhook 验签 + AES 解密 + 解析

淘宝消息服务对推送做**双重保护**：HTTP header 签名（防伪造）+ body AES-256-CBC 加密（防泄露）。两者都按官方《消息服务》文档校验。

```python
from fastapi import FastAPI, Request
import json, base64
from Crypto.Cipher import AES

app = FastAPI()

@app.post("/taobao/webhook")
async def taobao_webhook(request: Request):
    body = await request.body()
    # 1. 验签（按官方文档：对 app_secret + body 做摘要，与 header 中的签名比对）
    #    verify_taobao_signature(request.headers, body, app_secret)
    # 2. AES 解密（消息体是密文，按文档 AES-256-CBC + PKCS#7）
    #    plain = aes_decrypt(body["encrypt"], aes_key)  # aes_key = base64_decode(app_secret + "=")
    # 3. 解析明文（明文是 JSON，含消息列表）
    plain = json.loads(plain)  # 实际格式：{"messageList":[{"uuid":..., "content":..., "fromId":...}]}
    for m in plain.get("messageList", []):
        if m.get("messageType") == "ChatMessage":
            buyer_open_id = m["fromId"]
            content = m["content"]
            session_id = m.get("conversationId") or buyer_open_id
            # 4. 复用现有 Agent 逻辑
            reply = await run_chat(content, session_id)
            # 5. 回发
            await taobao_send_message(buyer_open_id, reply, m.get("shopId"))
    return {"code": 0, "msg": "success"}
```

### 4.3 发送客服消息（回发）

```python
import time, hashlib, httpx

async def taobao_send_message(buyer_open_id: str, content: str, shop_id: str):
    """调 taobao.openim.custmsg.push 发客服消息"""
    auth = await get_auth(shop_id)
    params = {
        "method": "taobao.openim.custmsg.push",
        "app_key": APP_KEY,
        "session": await auth.get_access_token(shop_id),
        "timestamp": time.strftime("%Y-%m-%d %H:%M:%S"),
        "format": "json",
        "v": "2.0",
        "sign_method": "hmac-sha256",
        # 业务参数
        "to_user": buyer_open_id,
        "content": json.dumps({"text": content}, ensure_ascii=False),
    }
    # TOP API 签名：把所有参数（除 sign）按字典序排序 + 拼接 app_secret 做 hmac-sha256
    params["sign"] = _sign_top(params, APP_SECRET)
    resp = await httpx.AsyncClient().post(
        "https://eco.taobao.com/router/rest",
        params=params,
    )
    return resp.json()
```

> ⚠️ 说明：上面字段名（`messageType=ChatMessage`、`fromId`、`conversationId`、AES key 派生方式、签名算法）是**示意**，落地时以淘宝开放平台最新《消息服务》文档为准。自用型应用能申请到的客服消息权限范围以「应用控制台 → 消息服务 → 订阅」页面为准，**部分接口可能对自用型应用不开放**，届时需走工具型/ISV 流程。

---

## 五、真实接入的硬门槛（诚实说明）

这几条**绕不过去**，写出来让你对成本有数：

| 门槛 | 说明 |
|---|---|
| 企业资质 | 淘宝开放平台需营业执照（个体工商户可入驻，个人开发者权限受限，**客服消息权限包通常要求企业资质**） |
| 淘宝/天猫店铺 | 必须有真实店铺（客服消息推送绑定店铺） |
| 自用型 / 工具型应用审核 | 创建应用 → 申请客服消息权限包 → 人工审核（通常 3~7 个工作日） |
| OAuth 授权 | 店铺主扫码授权一次，后续 `refresh_token` 续期 |
| 公网 HTTPS 地址 | 消息推送地址必须公网可访问、HTTPS；本地开发可用 frp / cpolar / ngrok 内网穿透 |
| 消息订阅配置 | 应用控制台 → 消息服务 → 订阅 `taobao_openim_ChatMessage` 事件，配置回调 URL |
| 消息加密 | 必须按官方 AES-256-CBC 解密 + 签名校验（双重），否则 webhook 视为无效 |
| API 限流 | TOP API 有 QPS 限制，客服消息高频场景需评估配额 |

**对你的现实意义**：你目前是学生、没有企业资质和淘宝店铺，**短期内无法走通「真实淘宝推送 → 你的服务器」这一步**（这一步卡在资质，不卡在技术）。

---

## 六、学生可落地的做法：适配层 + Mock 联调（推荐）

不造假、又能证明「系统具备接入淘宝客服消息的能力」的做法是：**把适配层写好，用 Mock 模拟器把协议链路在本机跑通**。

```
mock_taobao.py（本地模拟淘宝，向 webhook POST 加密 + 签名的客服消息）
        │
        ▼
taobao_webhook.py（验签 + AES 解密 + 解析 + 复用 run_chat + 回发）
        │
        ▼
mock 回执打印（模拟「taobao.openim.custmsg.push」成功）
```

交付物：
1. `taobao_adapter.py` — 淘宝消息的入站解密/验签、出站签名/加密、OAuth token 管理（真实协议格式）。
2. `mock_taobao.py` — 本地脚本，模拟淘宝推送一条加密客服消息（如「帮我查订单 TB20260812001 的物流」），并打印收到的回复。
3. `taobao_webhook.py` — FastAPI 路由，把消息喂给现有 Agent，回发回复。

链路验证通过后即具备接入淘宝客服消息的能力；真实接入所需的资质/店铺/审核流程按官方文档办理。

---

## 七、与抖音方案的差异点（避免落地时混用）

| 维度 | 抖音（抖店） | 淘宝 |
|---|---|---|
| 协议文档 | 抖店消息推送协议（自用型 token，7 天有效） | TOP API 消息服务（OAuth 2.0 + `refresh_token`，7~30 天有效） |
| 加密 | 签名校验（app_secret + body 摘要） | **签名 + AES-256-CBC 加密双重** |
| 应用类型 | 自用型应用 | 自用型 / 工具型 / ISV（**客服消息权限包对自用型应用限制较多**） |
| 授权方式 | `shop_id` + app_key/app_secret 直换 token | 店铺主扫码 OAuth 2.0 授权 + code 换 token |
| 接口风格 | 单一 REST 端点（`openapi-fxg.jinritemai.com`） | 统一 router（`eco.taobao.com/router/rest`）+ 按 `method` 参数分发 |
| 签名算法 | 抖店自定义 | **TOP 标准签名**：参数字典序排序 + hmac-sha256 |
| 回发接口 | `feige/message/send` | `taobao.openim.custmsg.push` |
| 字段含义 | `open_id` / `conversation_id` | `fromId` / `conversationId` / `messageType` |

**落地建议**：抖音和淘宝的适配层文件分开（`douyin_adapter.py` / `taobao_adapter.py`），共用 `run_chat()` 入口。webhook 路由用不同前缀区分（`/douyin/webhook` / `/taobao/webhook`）。

---

## 八、【待确认】清单

1. ~~【待确认】是否按「路径 A（TOP API 消息推送）」推进？还是你后续想做千牛插件嵌入工作台（路径 B）？~~  ✅ **2026-09-07 已确认：路径 A（TOP API）**
2. ~~【待确认】是否要我**现在落地第六节的适配层 + Mock 联调代码**（约 3 个文件，本机可跑通验证）？~~  ✅ **2026-09-07 已确认：现在就落 Mock 联调代码**
3. ~~【待确认】回发消息的策略：默认「AI 全自动回复」，还是「AI 生成回复 → 坐席一键确认后发送」（后者更稳、符合"AI 辅助 + 人工兜底"的定位）？~~  ✅ **2026-09-07 已确认：坐席一键确认后发送**（AI 生成回复后进坐席工作台待审，客服确认后调用 `taobao.openim.custmsg.push` 发出）
4. 【待确认】你后续是否会申请真实淘宝店铺（有营业执照）？如果有，我可以再补「应用创建 + 权限包申请 + OAuth 授权流程 + 内网穿透」的详细操作步骤。
5. ~~【待确认】是否要把现有 `douyin_adapter.py` / `douyin_webhook.py` / `mock_douyin.py` 的代码作为基线，复用同一套模式生成 `taobao_*.py`（保留差异点：加密 / 签名 / OAuth）？~~  ✅ **2026-09-07 已确认：复用抖音基线生成淘宝代码**

---

## 附：接入后的能力边界

- **多租户**：淘宝的 `seller_nick` 天然对应现有 `tenant_id`，一套系统可服务多个店铺，知识库按店铺隔离。
- **人机协作**：Triage 判定的高敏感/复杂诉求（索赔、投诉、转人工）自动进坐席工作台，坐席接管后消息不再走 Agent——和现有 `_manual_sessions` 机制完全复用。
- **可观测**：复用 `agent_trace`，每条淘宝咨询都记录「分流 → 工具调用 → 回复」轨迹，便于回溯和调优。
- **跨渠道**：未来如需同时接入抖店 + 淘宝 + 微信小程序的客服消息，共用 `run_chat()` 入口 + 适配器模式（每个渠道一个 `*_adapter.py`），业务侧无需改动。