# 抖音客服消息接入方案（ai-cs-agent）

> 目标：让 ai-cs-agent 的 6 Agent 客服系统，接入抖音/抖店的客服消息渠道，自动回复买家咨询。
> 状态：方案稿，含【待确认】项，等你拍板后再落地代码。

---

## 一、先厘清：抖音有「两个平台」，别走错门

很多人一说"接入抖音"就去找 `open.douyin.com` 的私信接口，这条路对电商客服是**错的**。先分清楚：

| 平台 | 网址 | 面向谁 | 和客服消息的关系 |
|---|---|---|---|
| 抖音开放平台 | open.douyin.com | App / 小程序 / 网站开发者 | 面向「私信/群聊」，**2025.4.14 起已对新开发者全面下线**，走不通 |
| 抖店开放平台 | op.jinritemai.com | **抖音电商商家** | 面向「店铺客服（飞鸽）消息」，是电商客服的正规入口 ✅ |

结论：**你的系统是电商客服（订单/商品/售后/政策），应该走「抖店开放平台」，不是「抖音开放平台」私信。**

---

## 二、三条可走的路，只有一条是对的

| 路径 | 做法 | 门槛 | 结论 |
|---|---|---|---|
| **A. 抖店客服消息（官方 API）** | 抖店开放平台「消息推送」把买家消息 webhook 推到你服务器 → 你调 Agent → 再调「发送客服消息」API 回复 | 企业资质 + 抖店店铺 + 应用审核 + 权限包 | ✅ **推荐**（正规、可规模化） |
| B. 抖音小程序客服（消息推送客服） | 小程序配置服务器 URL + Token，用户消息 POST 给你 → 你回复 | 小程序已上线 + 开发者认证 | ⚠️ 备选（适用于有小程序的产品） |
| C. 飞鸽后台 RPA 自动化 | 浏览器插件/脚本自动点飞鸽后台回复 | 无官方接口 | ❌ 不推荐（封号风险、灰色、非正规） |

**推荐路径 A 的理由：**
1. 抖店客服消息是官方开放 API，合规、稳定、可规模化。
2. ai-cs-agent 的 6 Agent（订单/商品/售后/政策）职能正好对应抖店客服的典型咨询场景，天然匹配。
3. 走 API 能拿到结构化的「买家 open_id + 消息内容 + 会话 ID」，可以和现有的 `session_id` 会话管理、多租户 `tenant_id` 对齐。

---

## 三、接入架构（核心原则：不侵入现有 Agent 逻辑）

现有 `/api/chat` 已经封装了完整链路「消息 → Triage 分流 → 业务 Agent → 回复 + trace + 转人工」。抖音接入**只加一层适配器**，把抖店消息格式翻译成内部格式，复用同一套 Agent 逻辑：

```
抖店买家发消息（飞鸽客服）
        │
        ▼  抖店「消息推送」webhook POST（需公网地址 + 验签）
┌─────────────────────────────────────────────┐
│ douyin_webhook.py   ← 新增，接收抖店推送        │
│   1. verify_signature()   验签                │
│   2. parse_message()      解析买家消息          │
│   3. 调 run_chat(msg, session_id) 复用现有逻辑  │
│   4. send_message()       调抖店 API 回发回复   │
└─────────────────────────────────────────────┘
        │                        ▲
        ▼                        │
   复用 main.py 的 Runner.run(triage_agent, ...)  → 返回 reply
```

**关键复用点**：把 `/api/chat` 里那段「`Runner.run(triage_agent, msg, context=ctx, max_turns=10)` + 异常处理 + trace 提取」抽成一个独立函数 `run_chat(msg, session_id)`，webhook 和 REST 接口共用，避免逻辑分叉。

**会话映射**：抖店的「会话 ID / 买家 open_id」→ 内部 `session_id`。复用现有的 `_sessions`（30 分钟过期）+ 转人工 `_manual_sessions` 机制，实现跨轮次上下文保留。

---

## 四、关键代码骨架（真实接口，落地时按官方文档补字段）

### 4.1 获取 access_token（OAuth2，自用型应用）

```python
import time, httpx

class DouDianAuth:
    def __init__(self, app_key: str, app_secret: str, shop_id: str):
        self.app_key = app_key
        self.app_secret = app_secret
        self.shop_id = shop_id
        self._token = None
        self._expire = 0.0

    async def get_access_token(self) -> str:
        # 内存缓存 + 过期前刷新（token 有效期约 7 天）
        if self._token and time.time() < self._expire:
            return self._token
        resp = await httpx.AsyncClient().post(
            "https://openapi-fxg.jinritemai.com/token/create",
            json={
                "app_key": self.app_key,
                "app_secret": self.app_secret,
                "grant_type": "authorization_self",   # 自用型
                "shop_id": self.shop_id,
            },
        )
        data = resp.json()
        self._token = data["data"]["access_token"]
        self._expire = time.time() + data["data"]["expires_in"] - 300  # 提前 5 分钟刷新
        return self._token
```

### 4.2 webhook 验签 + 解析（抖店消息推送协议）

```python
from fastapi import FastAPI, Request

app = FastAPI()

@app.post("/douyin/webhook")
async def douyin_webhook(request: Request):
    body = await request.body()
    # 1. 验签（按抖店文档：对 app_secret + body 做签名，防伪造）
    #    verify_signature(request.headers, body, app_secret)
    # 2. 平台会先 POST 一条测试消息，必须原样返回下面这句，否则无法启用推送
    if _is_test_message(body):
        return {"code": 0, "msg": "success"}

    # 3. 解析消息（tag 区分类型；客服消息取买家 open_id + 文本内容）
    msgs = json.loads(body)          # 实际是数组 [{tag, msg_id, data}]
    for m in msgs:
        if m["tag"] == CUSTOMER_SERVICE_MESSAGE_TAG:
            open_id = m["data"]["from_user_open_id"]
            content = m["data"]["content"]
            session_id = m["data"]["conversation_id"]
            # 4. 复用现有 Agent 逻辑
            reply = await run_chat(content, session_id)
            # 5. 回发
            await send_message(open_id, session_id, reply)
    return {"code": 0, "msg": "success"}
```

### 4.3 发送客服消息（回发）

```python
async def send_message(auth, open_id, conversation_id, content):
    await httpx.AsyncClient().post(
        "https://openapi-fxg.jinritemai.com/feige/message/send",  # 飞鸽客服消息发送，接口名以最新文档为准
        params={"access_token": await auth.get_access_token()},
        json={"to_user_open_id": open_id, "conversation_id": conversation_id,
              "content": content},
    )
```

> ⚠️ 说明：上面字段名（`feige/message/send`、`from_user_open_id`、`conversation_id`、消息 `tag` 常量）是**示意**，落地时以抖店开放平台最新《客服消息》文档为准。抖店客服消息的 push `tag` 是否已全量开放，需在应用后台「消息订阅」页面确认。

---

## 五、真实接入的硬门槛（诚实说明）

这几条**绕不过去**，写出来让你对成本有数：

| 门槛 | 说明 |
|---|---|
| 企业资质 | 抖店开放平台需营业执照（个体工商户可入驻，个人号不行） |
| 抖店店铺 | 需一个真实的抖店店铺，客服消息绑定店铺 |
| 开发者认证 + 应用审核 | 创建「自用型应用」→ 绑定店铺 → 申请客服消息相关权限包 → 人工审核 |
| 公网地址 | 消息推送地址必须公网可访问；本地开发可用 frp / cpolar / ngrok 内网穿透 |
| 接口调用量 | 有 API 限流，客服消息高频场景需评估配额 |

**对你的现实意义**：你目前是学生、没有企业资质和抖店店铺，**短期内无法走通「真实抖店推送 → 你的服务器」这一步**（这一步卡在资质，不卡在技术）。

---

## 六、学生可落地的做法：适配层 + Mock 联调（推荐）

不造假、又能证明「系统具备接入抖音客服消息的能力」的做法是：**把适配层写好，用 Mock 模拟器把协议链路在本机跑通**。

```
mock_douyin.py（本地模拟抖店，向 webhook POST 抖店格式的客服消息）
        │
        ▼
douyin_webhook.py（验签 + 解析 + 复用 run_chat + 回发）
        │
        ▼
mock 回执打印（模拟「发送客服消息」成功）
```

交付物：
1. `douyin_adapter.py` — 抖店消息的入站解析、出站封装、验签骨架（真实协议格式）。
2. `mock_douyin.py` — 本地脚本，模拟抖店推送一条客服消息（如「帮我查订单 TB20260812001 的物流」），并打印收到的回复。
3. `douyin_webhook.py` — FastAPI 路由，把消息喂给现有 Agent，回发回复。

链路验证通过后即具备接入抖音客服消息的能力；真实接入所需的资质/店铺/审核流程按官方文档办理。

---

## 七、【待确认】清单

1. 【待确认】是否按「路径 A（抖店客服消息）」推进？还是你手上有小程序、想走路径 B？
2. 【待确认】是否要我**现在落地第六节的适配层 + Mock 联调代码**（约 3 个文件，本机可跑通验证）？
3. 【待确认】回发消息的策略：默认「AI 全自动回复」，还是「AI 生成回复 → 坐席一键确认后发送」（后者更稳、符合你"AI 辅助 + 人工兜底"的定位）？
4. 【待确认】你后续是否会申请真实抖店（有营业执照）？如果有，我可以再补「应用创建 + 权限包申请 + 内网穿透」的详细操作步骤。

---

## 附：接入后的能力边界

- **多租户**：抖店的 `shop_id` 天然对应现有 `tenant_id`，一套系统可服务多个店铺，知识库按店铺隔离。
- **人机协作**：Triage 判定的高敏感/复杂诉求（索赔、投诉、转人工）自动进坐席工作台，坐席接管后消息不再走 Agent——和现有 `_manual_sessions` 机制完全复用。
- **可观测**：复用 `agent_trace`，每条抖店咨询都记录「分流 → 工具调用 → 回复」轨迹，便于回溯和调优。
