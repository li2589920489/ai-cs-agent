# 评测数据集选型与落地方案

> 目的：给「AI 电商多 Agent 客服系统」补上**量化评测**这块短板
> 现状：RAG 全链路已实现但只有功能级验证（23 chunk、5 条查询命中），没有标准指标数字
> 结论：**能找到，且分两条线**——RAG 检索质量评测 + Agent 意图路由评测

---

## 一、两条评测线（先想清楚测什么）

| 评测线 | 测的是什么 | 对应简历数字 | 用什么数据集 |
|--------|-----------|-------------|-------------|
| **RAG 检索质量** | BGE 向量 + BM25 混合检索 + RRF + 重排这条链路，能不能把「相关文档」排在前面 | Recall@k / MRR / nDCG | C-MTEB 的检索子集（**EcomRetrieval 首选**） |
| **Agent 意图路由** | Triage 分流是否把用户问题路由到正确的 Agent | 意图识别准确率 / 混淆矩阵 | JDDC / SMP-ECDT / **自建评测集** |

> 关键认知：你项目的知识库是**模拟数据**（4 商品、3 订单、7 条店铺政策），而公开数据集是**真实电商语料**，两者不能直接互灌。所以公开集用来「证明 RAG 组件选型正确」，自建集用来「证明你的系统业务上好用」。

---

## 二、数据集清单（已核实，国内镜像可达）

### 2.1 RAG 检索 / 重排评测（C-MTEB，HuggingFace `C-MTEB/xxx`，hf-mirror 可达）

| 数据集 | HF 路径 | 规模 | 领域 | 贴合度 | 指标 |
|--------|---------|------|------|--------|------|
| **EcomRetrieval** ⭐ | `C-MTEB/EcomRetrieval` | 1,000 query | **阿里电商搜索引擎** | ★★★★★ 最贴合 | Recall@k / MRR / nDCG |
| DuRetrieval | `C-MTEB/DuRetrieval` | 4,000 query | 百度网页检索 | ★★★ 通用兜底 | 同上 |
| MMarcoRetrieval | `C-MTEB/MMarcoRetrieval` | 7,437 query | MS MARCO 中文版 | ★★★ | 同上 |
| T2Retrieval | `C-MTEB/T2Retrieval` | 24,832 query | 中文段落排序 | ★★★ 大而全 | 同上 |
| **T2Reranking** ⭐ | `C-MTEB/T2Reranking` | 24,382 对 | 中文段落排序 | ★★★★ 专门测重排 | MRR / MAP |
| MMarcoReranking | `C-MTEB/Mmarco-reranking` | 7,437 对 | 中文重排 | ★★★ | MRR / MAP |

> **推荐**：`EcomRetrieval`（电商中文，直接命中你的领域）+ `T2Reranking`（专门验证 bge-reranker 重排是否有效）。这两个下载量小、跑得快、结论最有说服力。

### 2.2 Agent 意图路由评测（电商客服对话）

| 数据集 | 获取方式 | 规模 | 意图标注 | 语言 | 门槛 |
|--------|---------|------|---------|------|------|
| **JDDC 2019** ⭐ | `jddc.jd.com` 申请授权 | 100 万对话 / 2000 万话语 | 289 意图 | 中文售后 | ★★★ 高（学术限用、需签协议） |
| JDDC 2.0 | 同上 | 13 万对话 + 图片 | 意图（图） | 中文多模态 | 高 |
| SMP-ECDT 2017/2018 | GitHub `kayakTEL/SMP2017ECDT-DATA` | 万级 | 31 意图 | 中文通用域 | ★ 低（直接开源） |
| Amazon ESCI | GitHub `amazon-science/esci-data` | 130k query / 260 万标注 | ESCI 四级相关性 | 英/西/日 | ★ 低（Apache-2.0 可商用） |

> **注意**：JDDC 是「售后客服意图」最贴合的，但**获取门槛高**（官网申请、仅限学术研究、禁止商用、需签协议），且原官网近年可能不稳定。SMP-ECDT 开源易得，但意图是「订机票/酒店/公交」这类**通用对话域，不是电商售后**。

---

## 三、推荐方案（直接照做）

**主线（先做，性价比最高）：用 EcomRetrieval 跑 RAG 检索标准指标**
- 立刻可做，数据在国内镜像能下，跑得快
- 产出 `Recall@5 / MRR@10 / nDCG@10` 三个硬数字，直接写进简历
- 同时跑一个「重排 on/off 对比」：证明 reranker 让指标提升多少（这是最有面试价值的一张对比表）

**辅线（第二步）：自建 50~100 条 Agent 路由评测集**
- 基于你自己的 6 个 Agent 边界（订单/商品/售后/政策/转人工/闲聊），手写 query + 标注正确路由
- 用它测 Triage 分诊准确率，输出混淆矩阵
- JDDC/SMP-ECDT 作为「扩展语料」参考，不强制依赖（门槛高/领域偏）

**第三步（可选）：Amazon ESCI**
- 商品搜索相关性金标准，但英文。想展示「跨语言/通用检索能力」或做商品搜索方向时才加

---

## 四、落地步骤（RAG 检索评测）

### Step 1：下载 EcomRetrieval

```bash
cd "D:\下载应用\个人ai训练中心\ai-cs-agent\python-backend"
export HF_ENDPOINT=https://hf-mirror.com

# 方式 A：用 huggingface-cli 下载到本地目录
huggingface-cli download --repo-type dataset C-MTEB/EcomRetrieval \
  --local-dir data/eval/EcomRetrieval --local-dir-use-symlinks False

# 方式 B：用 datasets 库直接加载（会缓存到 ~/.cache/huggingface）
.venv/Scripts/python.exe -c "from datasets import load_dataset; d=load_dataset('C-MTEB/EcomRetrieval'); print(d)"
```

### Step 2：指标定义（面试也要能背）

- **Recall@k**：相关文档在检索结果 top-k 里的比例（召回有没有漏）
- **MRR@10**：第一个相关文档「排名的倒数」取平均（第一个对的排多靠前）
- **nDCG@10**：考虑排名位置的相关性（对的分越高、越靠前越好）

### Step 3：评测脚本框架（复用你现成的 pipeline.search）

```python
# eval_retrieval.py —— 对公开检索集算 Recall@k / MRR / nDCG
from rag.pipeline import RAGPipeline

def eval_retrieval(pipeline, queries, qrels, k=5):
    """queries: {qid: text}; qrels: {qid: {relevant_chunk_id: score}}"""
    recall, mrr, ndcg = [], [], []
    for qid, text in queries.items():
        hits = pipeline.search(text, k=k)          # 返回 [{text, metadata, score}]
        # metadata["_idx"] 是 chunk 下标，映射回 corpus id
        got = [h["metadata"]["_idx"] for h in hits]
        rel = set(qrels.get(qid, {}).keys())
        # Recall@k
        recall.append(len(set(got) & rel) / len(rel) if rel else 0)
        # MRR
        for rank, g in enumerate(got, start=1):
            if g in rel:
                mrr.append(1.0 / rank); break
        else:
            mrr.append(0.0)
        # nDCG@k（简化为相关=1）
        dcg = sum(1.0 / __import__('math').log2(r+1) for r, g in enumerate(got, 1) if g in rel)
        idcg = sum(1.0 / __import__('math').log2(r+1) for r in range(1, min(len(rel), k)+1))
        ndcg.append(dcg / idcg if idcg else 0)
    return {
        f"recall@{k}": sum(recall)/len(recall),
        f"mrr@{k}": sum(mrr)/len(mrr),
        f"ndcg@{k}": sum(ndcg)/len(ndcg),
    }
```

> 重点：**同一份数据集，跑两次**——`use_hybrid=False`（纯向量）vs `use_hybrid=True`（混合+RRF）、`use_rerank=False` vs `True`，产出一张对比表，直观证明「混合检索 > 纯向量」「重排 > 不重排」。这是简历里最硬的量化证据。

---

## 五、自建 Agent 路由评测集（模板）

```json
[
  {"query": "我的订单怎么还没发货", "expected_agent": "订单详情 Agent"},
  {"query": "这件衣服掉色能退吗", "expected_agent": "售后退换货 Agent"},
  {"query": "你们家满 200 减多少", "expected_agent": "店铺政策 Agent"},
  {"query": "这个手机续航怎么样", "expected_agent": "商品知识 Agent"},
  {"query": "我要投诉你们客服", "expected_agent": "Human Escalation Agent"},
  {"query": "今天天气怎么样", "expected_agent": "Triage Agent（拒答/无关）"}
]
```

- 每个 Agent 类别写 8~15 条，共 50~100 条
- 覆盖边界用例：售后里「食品退货」「金额争议」这类应转人工的、商品和政策易混淆的（「满减」是政策不是商品）
- 跑完输出 **Accuracy + 混淆矩阵**，看哪两个 Agent 最容易分错（大概率是「商品知识 vs 店铺政策」）

---

## 六、诚实边界（面试口径）

| 可能的追问 | 诚实回答 |
|-----------|---------|
| "EcomRetrieval 是你的数据吗？" | 不是，是 C-MTEB 的公开电商检索基准，我拿它来验证 RAG 组件（BGE/混合检索/重排）的检索质量 |
| "有没有端到端业务准确率？" | 检索有标准指标数字；端到端意图路由用自建 50~100 条评测集测的准确率（诚实报数字） |
| "JDDC 用了吗？" | 没直接用，因为学术限用 + 官网申请门槛高；我用自建评测集 + 公开检索基准替代 |

---

*下一步：确认后我可以直接帮你下载 EcomRetrieval + 写 eval_retrieval.py 跑出第一版 Recall/MRR/nDCG 数字。*
